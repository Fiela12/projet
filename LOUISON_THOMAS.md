#Commande Thomas LOUISON

```shell
git clone https://github.com/Fiela12/projet
cd evaluation
touch LOUISON_THOMAS.md
git config user.email "ivetui78@gmail.com"
git config user.name "AFMAsa"
git add LOUISON_THOMAS.md
git commit -m "Fichier de commande pour Thomas"
git push origin main
  AFMAsa
  mon token

git branch CSS
git push origin CSS
  AFMAsa 
  mon token
  
git pull origin accueil
	cd accueil
	nano index.html (pour voir le code)

git checkout CSS
touch style.css 
nano style.css
	j'écris le code css
git add style.css
git commit "fichier css"
git push origin CSS
```git 
